# Notes

## Stage

* `frame` - `{ x, y }`
* `zoom`
* `nodes` - collection

## Collection

* `frame` - `{ x, y, width, height }` derived
* all - array

## Group

* `frame` - `{ x, y }`
* `nodes` - collection

## Area

* `frame` - `{ x, y }`
* `nodes` - collection

## Node

* `frame` - `{ x, y, width, height }`
