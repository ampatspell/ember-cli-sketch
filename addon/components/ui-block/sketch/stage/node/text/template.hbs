<div class="text" contenteditable={{if isEditing 'true' 'false'}}>{{text}}</div>
{{#if isEditing}}
  <div class="editing"></div>
{{/if}}
{{#if isLoading}}
  <div class="overlay">
    <div class="label">Loading font…</div>
  </div>
{{/if}}
{{ui-block/sketch/stage/deferred promise=loader.promise}}
